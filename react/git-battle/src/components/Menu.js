import React from 'react';
import {MenuStyle} from '../assets/styles/Menu';

const Menu = () => {
    let content = (
        <MenuStyle>
            <span> No content available. </span>
        </MenuStyle>
    );
    return content;
}

export default Menu;