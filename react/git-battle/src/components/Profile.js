import React from 'react';
import Menu from '../components/Menu';

const Profile = () => {
    let content = (
        <div>
            <Menu container= {"profile"} />
        </div>
    );
    return content;
}

export default Profile;