import React from 'react';
import Menu from './Menu';

const Home = () => {
    let content = (
        <div>
            <Menu container={"home"}/>
        </div>
    );
    return content;
}

export default Home;