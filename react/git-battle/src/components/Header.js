import React from 'react';
import { HeaderStyle } from '../assets/styles/Header.js'

const Header = () => {
    let content = (
        <HeaderStyle>
            <span> Git Battle ⚔️ </span>
        </HeaderStyle>
    );
    return content;
}

export default Header;